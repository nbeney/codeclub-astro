import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://icantech.org',  // Replace with your GitHub Pages URL
  base: '/codeclub-astro',  // Replace with your repository name
});
